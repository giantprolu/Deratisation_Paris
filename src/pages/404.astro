---
import Layout from '../layouts/Layout.astro';
import { Home, ArrowLeft } from 'lucide-react';
---

<Layout
  title="404 - Page non trouvée"
  description="La page que vous recherchez n'existe pas ou a été déplacée."
  canonical="/404"
>
  <div class="min-h-screen bg-gradient-to-b from-gray-50 to-gray-100 flex items-center justify-center px-4">
    <div class="max-w-lg w-full text-center">
      <div class="relative">
        <h1 class="text-9xl font-bold text-gray-200">404</h1>
        <p class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 text-2xl font-semibold text-gray-900">
          Page non trouvée
        </p>
      </div>
      
      <p class="mt-6 text-gray-600 text-lg">
        Oups! La page que vous recherchez semble s'être égarée dans le désert numérique.
      </p>

      <div class="mt-10 flex justify-center space-x-4">
        <button
          onclick="history.back()"
          class="flex items-center px-6 py-3 bg-gray-800 text-white rounded-lg hover:bg-gray-700 transition-colors duration-200"
        >
          <ArrowLeft className="w-5 h-5 mr-2" />
          Retour
        </button>
        
        <a
          href="/"
          class="flex items-center px-6 py-3 bg-primary-600 text-white rounded-lg hover:bg-primary-dark transition-colors duration-200"
        >
          <Home className="w-5 h-5 mr-2" />
          Accueil
        </a>
      </div>
    </div>
  </div>
</Layout>
